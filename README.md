# Task Tracker with SQL

This is a simple task tracking web application built using:

- Node.js (Express)
- SQLite (SQL database)

## Features
- Add tasks
- View tasks
- Delete tasks
- SQL database integration

## SQL Usage
The project uses SQL queries such as:
- CREATE TABLE
- INSERT INTO
- SELECT
- UPDATE
- DELETE

## Run Locally
npm install  
node server.js